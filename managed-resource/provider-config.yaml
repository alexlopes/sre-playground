apiVersion: helm.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: InjectedIdentity
  # Optional: Configure Helm client settings
  # helmClientConfig:
  #   # Maximum time to wait for any individual Kubernetes operation (like Jobs for hooks)
  #   timeout: 300s
  #   # Enable debug logging
  #   debug: false
  #   # Kubernetes API server burst limit
  #   burst: 100
  #   # Kubernetes API server QPS limit  
  #   qps: 50